// See https://aka.ms/new-console-template for more information
using Katas.ArrayDiff;
using Katas.Oddint;

// [0,1,0,1,0] should return 0, because it occurs 3 times (which is odd).
int[]array = [0,1,0,1,0];

Console.WriteLine(Katas.Oddint.Kata.find_it(array));
